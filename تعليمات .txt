اولا شرح الكود 

تمام 👍. سأشرح لك هذا الكود خطوة خطوة حتى تفهم تمامًا ما يقوم به
.


---

## **ملخص عام**

الكود عبارة عن تطبيق **تحديد قارئ القرآن الكريم** باستخدام **تحليل الصوت** وواجهة **Streamlit** لعرض النتائج.
يقوم الكود بتحميل قاعدة بيانات تحتوي على معلومات عن قراء (اسم، رابط صوتي، مصدر)، ثم يستخرج ميزات صوتية من التسجيلات (MFCC)، ويحاول مقارنة التسجيل الصوتي المرفوع مع التسجيلات في قاعدة البيانات باستخدام **مسافة كوساين (Cosine Distance)** لمعرفة أقرب قارئ.

---

### **1. المكتبات المستخدمة**

* `os`: التعامل مع الملفات والمجلدات.
* `librosa`: تحليل ومعالجة الصوت.
* `numpy`: عمليات رياضية على المصفوفات.
* `requests`: تحميل ملفات صوتية من الإنترنت.
* `json`: قراءة وكتابة قاعدة البيانات.
* `streamlit`: إنشاء واجهة تفاعلية للتطبيق.
* `scipy.spatial.distance.cosine`: لحساب التشابه بين الميزات الصوتية.

---

### **2. الإعدادات**

```python
DATA_FILE = "reciters_db.json"
FEATURES_DIR = "features_cache"
TEMP_AUDIO = "temp_upload.wav"

os.makedirs(FEATURES_DIR, exist_ok=True)
```

* **DATA_FILE**: ملف JSON يحتوي على قاعدة بيانات القراء.
* **FEATURES_DIR**: مجلد لتخزين الميزات الصوتية المحسوبة (MFCC).
* **TEMP_AUDIO**: اسم ملف مؤقت للتسجيل الصوتي المرفوع.
* ينشئ مجلد `features_cache` إذا لم يكن موجودًا.

---

### **3. استخراج الميزات الصوتية**

```python
def extract_features(file_path):
    y, sr = librosa.load(file_path, sr=None)
    mfcc = librosa.feature.mfcc(y=y, sr=sr, n_mfcc=13)
    mfcc_mean = np.mean(mfcc, axis=1)
    return mfcc_mean.tolist()
```

* يقوم بتحميل الملف الصوتي.
* يستخرج **MFCC (Mel-Frequency Cepstral Coefficients)** وهي ميزات صوتية مهمة لتحليل الأصوات.
* يحسب المتوسط عبر الوقت للحصول على **متجه ميزات ثابت الطول**.

---

### **4. تحميل الصوت من الإنترنت**

```python
def download_audio(url, save_path):
    response = requests.get(url)
    response.raise_for_status()
    with open(save_path, "wb") as f:
        f.write(response.content)
```

يحمل ملف صوتي من رابط ويحفظه على القرص.

---

### **5. التعامل مع قاعدة البيانات**

```python
def load_database():
    ...
def save_database(db_data):
    ...
```

* `load_database`: يقرأ قاعدة البيانات من ملف JSON.
* `save_database`: يحفظ قاعدة البيانات إلى ملف JSON.

---

### **6. حفظ وتحميل الميزات الصوتية**

```python
def save_features(name, new_features):
    ...
def load_features(name):
    ...
```

* كل قارئ له ملف JSON منفصل في مجلد `features_cache` لتخزين الميزات الصوتية.
* يسمح بإضافة ميزات جديدة أو تحميل الميزات الموجودة.

---

### **7. بناء قاعدة ميزات الصوت**

```python
def build_features_db():
    ...
```

* يقرأ قاعدة البيانات.
* إذا لم تكن ميزات القارئ موجودة، يقوم بتحميل الصوت، استخراج الميزات، وتخزينها.
* يعيد قاعدة بيانات مع الميزات الصوتية.

---

### **8. التعرف على القارئ**

```python
def recognize_reciter(test_features, features_db):
    ...
```

* يقارن الميزات الصوتية للمقطع المرفوع مع ميزات كل قارئ في قاعدة البيانات.
* يستخدم **مسافة كوساين** لتحديد درجة التشابه.
* يرجع القارئ الذي لديه أقل مسافة (أي أقرب تطابق).

---

### **9. واجهة المستخدم باستخدام Streamlit**

```python
st.set_page_config(...)
```

* يضبط إعدادات الصفحة.
* يدعم **RTL** (اتجاه النص من اليمين لليسار).

#### رفع الملف الصوتي:

```python
uploaded_file = st.file_uploader(...)
```

* المستخدم يرفع ملف صوتي.
* يتم حفظه مؤقتًا ثم تحليل الميزات.

#### التعرف على القارئ:

```python
if st.button("تحديد القارئ"):
    ...
```

* يضغط المستخدم زر **"تحديد القارئ"**.
* يقوم ببناء قاعدة الميزات، استخراج ميزات المقطع المرفوع، ثم التعرف على القارئ.
* يعرض النتائج.

---

### **10. إضافة قارئ جديد**

```python
st.header("➕ إضافة قارئ جديد")
```

* يتيح للمستخدم إدخال:

  * اسم القارئ.
  * رابط الصوت الخاص به.
  * مصدر الصوت.
* يقوم بتحميل الصوت، استخراج الميزات، تخزينها في قاعدة البيانات.

---

✅ **الخلاصة:**
هذا الكود هو **نظام للتعرف على قارئ القرآن الكريم** باستخدام تقنية تحليل الصوت (MFCC) وقياس التشابه (Cosine Distance)، مع واجهة مستخدم جميلة باستخدام Streamlit تتيح رفع مقاطع صوتية أو إضافة قراء جدد.



ثانيا شرح كيفية اضافة و تحديث القراء محليا 

تمام 👍 فهمت سؤالك. أنت تريد **إضافة ميزات (features) جديدة لقارئ معين** أو إضافة قارئ جديد، ثم ترفع فقط الميزات الجديدة بدون إعادة رفع كل المشروع. سأوضح لك ما تحتاج تغييره أو تحديثه.

---

## **تحليل الكود الحالي**

الكود لديك يفصل الميزات (features) في ملف منفصل داخل مجلد **`features_cache`**:

* قاعدة البيانات (`reciters_db.json`) تخزن بيانات القارئ (الاسم، رابط الصوت، المصدر).
* الميزات الخاصة بكل قارئ مخزنة في:

```
features_cache/{name}.json
```

هذا يعني أنك لا تحتاج رفع المشروع بأكمله، فقط تحديث الملفات:

1. **ملف الميزات** (`features_cache/{name}.json`)
2. **قاعدة البيانات** (`reciters_db.json`) إذا أضفت قارئ جديد أو عدلت بيانات قارئ موجود.

---

## **الملفات التي يجب تحديثها**

إذا أضفت ميزات جديدة لقارئ موجود:

* فقط **features_cache/{name}.json** يحتاج تحديث.
  لا حاجة لمسح قاعدة البيانات، لأن قاعدة البيانات تحتوي على بيانات القارئ فقط، وليس الميزات.

إذا أضفت قارئ جديد:

* **reciters_db.json** → لإضافة بيانات القارئ (الاسم، رابط الصوت، المصدر).
* **features_cache/{name}.json** → لتخزين الميزات الخاصة به.

---

## **كيفية التدريب اليدوي وإضافة الميزات**

إذا أردت تدريب البوت بنفسك وإضافة أكبر عدد ميزات:

1. جهز ملف صوت لكل قارئ.
2. استخدم الكود الخاص بالاستخراج (`extract_features`) لحساب الميزات.
3. خزّن الميزات يدوياً في ملف JSON داخل مجلد `features_cache/`.

مثال:

```python
import json
from your_code import extract_features

features = extract_features("audio_of_reciter.wav")

with open("features_cache/reciter_name.json", "w", encoding="utf-8") as f:
    json.dump([features], f, ensure_ascii=False, indent=4)
```

يمكنك تكرار العملية لأي عدد من الميزات لكل قارئ.

---

## **رفع الميزات فقط**

بما أن الميزات مخزنة في مجلد `features_cache` وبيانات القارئ في `reciters_db.json`، فأنت تحتاج:

* رفع **features_cache/** بالكامل أو الملف الخاص بالقارئ الذي أضفت له ميزات.
* رفع `reciters_db.json` إذا أضفت قارئ جديد.

📌 ملاحظة: إذا كنت تستخدم **Streamlit Cloud** مع GitHub، فتأكد أن المجلد `features_cache/` موجود في المشروع على GitHub، وإلا لن يتم تحميل الميزات عند نشر التطبيق.

---

✅ الخلاصة:

* **لإضافة ميزات جديدة لقارئ موجود**: تحديث فقط `features_cache/{name}.json` ورفعها.
* **لإضافة قارئ جديد**: تحديث `reciters_db.json` + إنشاء `features_cache/{name}.json`.

---
ثالثا موقع القران الكريم 
تستطيع من خلاله اضافة المميزات الى التلاوات محليا فقط 
https://www.quranicaudio.com/

رابعا رابط موقع التجربة المنشور
https://reco-q-rec.streamlit.app/
